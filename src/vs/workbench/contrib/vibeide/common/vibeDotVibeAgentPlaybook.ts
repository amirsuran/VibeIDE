/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/


/**
 * Baseline instructions for any model: how to map natural-language requests to `.vibe/` files.
 * Appended to GUIDELINES when a workspace folder is open (see ConvertToLLMMessageService).
 * Project `.vibe/rules.md` / `AGENTS.md` may override or refine this text.
 */
export const VIBE_DOTVIBE_AGENT_PLAYBOOK = `
## VibeIDE — намерения из чата → папка \`.vibe/\` (обязательно следовать)

Работай от **корня соответствующей папки воркспейса** (при multi-root — уточни, к какому корню относится запрос).

### Перенос правил из Cursor в VibeIDE
Если пользователь просит забрать/объединить правила Cursor:
1. Прочитай через инструменты: \`.cursorrules\` (если есть), **\`.cursor/rules/**\` (все \`.mdc\` / \`.md\`), при необходимости — **пользовательский** каталог Cursor: \`%USERPROFILE%\\.cursor\\rules\` (Windows) или \`~/.cursor/rules\` (macOS/Linux).
2. Слей содержимое в **один** документ: убери дубликаты, конфликты разреши в пользу более свежей/явной формулировки, **вырежи секреты и локальные пути**.
3. Запиши итог в **\`.vibe/rules.md\`** (дополни существующий файл или замени целиком **только** если пользователь явно просит «замени всё»).
4. Если в репозитории есть \`scripts/vibe-init-from.js\`, можно предложить пользователю запустить его для полуавтоматического импорта — но **не считай** это заменой чтения файлов через инструменты, если пользователь ждёт результата в чате.

### Цели на день / спринт → \`.vibe/goals.md\`
**Единственный файл целей — корневой \`.vibe/goals.md\`.** НЕ создавай подпапки (\`.vibe/goals/…\`) и НЕ заводи отдельные файлы под цели. Если файл уже существует — обновляй его, не создавай новый. Его текущее содержимое уже дано тебе в блоке \`<session_goals>\`.
Когда пользователь формулирует цели («на сегодня», фокус сессии, спринт):
1. Сначала **прочитай** текущий \`.vibe/goals.md\` (если есть).
2. Структурируй текст: заголовок с датой/периодом, блок **Цели**, **Не делаем (non-goals)**, **Критерии готовности**.
3. Запиши в **\`.vibe/goals.md\`** (аккуратно смерджи или перезапиши по явной просьбе пользователя).
4. Если формулировка расплывчатая — **задай уточняющие вопросы** до записи.

### Планы → \`.vibe/plans/\`
На запросы «создай план», «оформи план»: создай файл **\`.vibe/plans/<краткий-slug>.plan.md\`** (латиница и дефисы в имени), с понятными секциями (контекст, шаги, риски). Если не ясно имя или scope — спроси.

### Workflow → \`.vibe/workflows/\`
На «создай workflow», «воркфлоу»: создай JSON **\`.vibe/workflows/<имя>.json\`** по образцу существующих (поля \`vibeVersion\`, \`name\`, \`description\`, \`steps[]\` с \`name\` и \`description\`). Сообщи пользователю вызов **\`/workflow:<имя>\`**. Если схема или шаги неочевидны — уточни.

### Когда информации недостаточно
Если не определены: целевой **корень** воркспейса, **замена vs merge** файла, **язык** секций, **секретность** (можно ли коммитить) — **остановись и спроси**; не выдумывай.

### Не дублировать зря
Если правило уже есть в \`.vibe/rules.md\`, не копируй его в другие файлы без причины.

### Защита целей от агента (опционально)
По умолчанию агент **может** править \`.vibe/goals.md\`. Если пользователь хочет запретить это — добавь в \`.vibe/constraints.json\` правило \`deny_write\` на \`.vibe/goals.md\` (тогда правки только вручную / через UI).
`.trim();
