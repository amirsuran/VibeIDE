/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/


/**
 * «Что нового» (What's New) — curated, hand-written highlights per release, shown once in a modal
 * after an update (see `browser/vibeWhatsNewContribution.ts`). Bundled as code (no I/O, no path
 * resolution across dev/installed/portable), keyed by `vibeVersion`. A version WITHOUT an entry
 * shows nothing — patch releases that need no announcement simply omit a key.
 *
 * Pure module → unit-testable. Content is Markdown (rendered via the modal's `bodyMarkdown`).
 *
 * RELEASE STEP: add an entry for the new version here when its changelog is worth surfacing.
 */

/** Markdown highlights keyed by exact `vibeVersion` (e.g. "1.1.0"). */
export const WHATS_NEW_BY_VERSION: Readonly<Record<string, string>> = {
	'1.5.4': [
		'## ✨ Новое',
		'',
		'- **«Токены перестали приходить» чинится само.** Если сетевые сбои идут сразу у двух и более провайдеров (сигнатура залипшего общего соединения), VibeIDE автоматически пересоздаёт LLM-транспорт — то же, что кнопка-ключ в статус-баре, но без вас; уведомление с кнопкой «Проверка провайдеров», не чаще раза в 10 минут, настройка `vibeide.llm.autoResetTransport`. Бонус: смена настроек провайдера (включая заголовки в `.vibe/providers.json`) теперь подхватывается без перезапуска IDE.',
		'- **MiniMax-M3: родные режимы размышлений.** Слайдер «мышление» теперь говорит с моделью на её языке: `adaptive` (модель сама решает, думать ли — экономит токены на простых шагах) / `enabled` (думать всегда) / выключено. Раньше слайдер для M3 не действовал вовсе — у API MiniMax нет `reasoning_effort`.',
		'- **Видно, какой слой «тащит» капризную модель.** В Настройках (Безопасность) — живые счётчики слоёв нормализации tool-call: какая форма нестандартных вызовов у текущей модели и какая защита её чинит; кнопки «Обновить» и «Сбросить счётчики» для проверки конкретной модели.',
		'',
		'## 🔒 Безопасность',
		'',
		'- **Редакция секретов теперь и в логах main-процесса.** Настройки `vibeide.logging.*` (уровень, категории, wildcard-группы) действуют живьём на все процессы, а ключи/токены вычищаются во всех приёмниках лога — консоль, буфер «копировать логи», Output-канал, файл.',
	].join('\n'),
	'1.5.3': [
		'## ✨ Новое',
		'',
		'- **Минимализм кода — агент больше не переусложняет.** Режимы **лайт/фулл/ультра** (`vibeide.global.minimalismMode`): сначала переиспользовать существующее (кодбаза → stdlib → зависимости) и лишь потом писать новое — меньше токенов, дешевле и быстрее. Команда `/simplify` возвращает делит-лист по диффу, а отложенные упрощения (`vibe-later`) собираются в леджер одной командой.',
		'- **Занятый порт проекта — под контролем.** Если порт из конфигурации проекта занят и dev-сервер уехал на соседний (или упал с EADDRINUSE), VibeIDE покажет окно: **освободить порт** (завершить занявший процесс и перезапустить), **работать на подменённом порту** (только эта сессия — конфигурация проекта не меняется) или **отмена**. Если порт держит dev-сервер другого вашего проекта в этой же VibeIDE — окно назовёт проект и по умолчанию предложит сосуществование, а не убийство соседа. Подменённый порт помечается в статус-баре: `3001 (вместо 3000)`.',
		'- **Добавить/убрать из .gitignore** — правой кнопкой на файле или папке в проводнике (работает с мультивыделением) либо на табе редактора: больше не нужно прописывать пути руками. Записи якорные, дубликаты не плодятся; wildcard-шаблоны не трогаются.',
	].join('\n'),
	'1.5.2': [
		'## 🐛 Исправления',
		'',
		'- **Vibe Server на macOS: dev-сервер снова запускается.** Приложению, открытому из Dock/Finder, не доставался PATH вашего шелла, поэтому `npm run dev` мгновенно падал с «код 127». Теперь VibeIDE подхватывает окружение логин-шелла и сам находит Node в стандартных местах установки (fnm/nvm/volta/Homebrew/nodejs.org), а если Node.js действительно не установлен — прямо говорит об этом и подсказывает, что сделать.',
	].join('\n'),
	'1.5.0': [
		'## ✨ Vibe Server — увидеть код локально, без деплоя',
		'',
		'- **Новая панель «Vibe Server»** (иконка рядом с Vibe Projects): поднимает локальный сервер и показывает результат прямо в IDE или во внешнем браузере — на выбор.',
		'- **Три режима под проект:** статический сайт (HTML/CSS/JS), **dev-сервер фреймворка** (Vite/Next/React/Vue/Angular — запускается ваш `npm run dev` и берётся его URL) и **Docker-окружение** (`docker compose` / Dockerfile — одной кнопкой «Поднять окружение»).',
		'- **Живая перезагрузка:** правка файла сразу обновляет превью; CSS применяется без перезагрузки страницы (состояние и прокрутка сохраняются).',
		'- **Встроенный браузер** с адресной строкой, кнопками назад/вперёд/обновить, размерами под телефон/планшет/десктоп и поворотом. Можно открыть несколько вкладок и синхронизировать прокрутку.',
		'- **Превью на телефоне:** команда показывает адрес в локальной сети и **QR-код** — отсканировал и открыл.',
		'- **Агент видит результат:** ошибки консоли и проваленные сетевые запросы из превью одной командой уходят в чат — агент сам их чинит. Счётчик ошибок виден в строке состояния.',
		'',
		'## 🚀 Vibe Deploy — выкатить в облако',
		'',
		'- **Деплой проекта в облако** (Timeweb Cloud первым; провайдеро-агностично) через агента: анализ проекта → Docker → инфраструктура → домен/SSL → CI/CD. Перед любым внешним действием спрашивает подтверждение, секреты не уходят в репозиторий.',
		'',
		'## 🤖 Vibe Agents — команда ролей',
		'',
		'- **Набор ролей-субагентов** (планировщик, дизайнер, фронтенд/бэкенд, ревьюер, QA, security) с правами на роль: ревьюер и security — только чтение, без записи. Команда «Показать маршрут ролей» подскажет, кто и в каком порядке возьмётся за задачу; security подключается автоматически на чувствительных темах (OAuth, платежи, секреты).',
	].join('\n'),
	'1.4.1': [
		'## ✨ Новое',
		'',
		'- **Кнопка «вниз» в чате.** Пока агент отвечает, чат сам держится у низа; если отлистали вверх читать историю — справа внизу появляется кнопка прокрутки к последнему сообщению.',
		'',
		'## 🐛 Исправления',
		'',
		'- **Слабые модели больше не зацикливаются на отступах.** При правке файла, если модель присылала фрагмент без отступа первой строки, инструмент терял этот отступ — и модель пыталась «починить пробелы» снова и снова. Теперь отступ автоматически выравнивается по файлу, а модель получает явный сигнал об этом и не уходит в петлю.',
		'- Полоска «в очереди» над полем ввода больше не перекрывается кнопками «развернуть»/Zen.',
	].join('\n'),
	'1.4.0': [
		'## 🔔 Уведомления, когда агент закончил, а вы отошли',
		'',
		'- **Иконка VibeIDE в панели задач мигает**, когда агент завершил работу, остановился или ждёт вашего ответа, а окно свёрнуто или в фоне. Помигает и спокойно подсветится — не отвлекает, но забыть не даёт. Клик по иконке возвращает к IDE и переключает на её рабочий стол.',
		'- **Системный тоаст** с тем же сообщением; клик по нему тоже возвращает к VibeIDE.',
		'- Всё это в дополнение к звуковому сигналу и срабатывает, только когда окно не в фокусе. Отключается тумблером в разделе «Уведомления».',
		'',
		'## 💬 Очередь сообщений во время хода агента',
		'',
		'- Пока агент работает, набранный текст по Enter теперь **сразу виден закреплённой полоской над полем ввода** — понятно, что встало в очередь и подмешается на следующем шаге агента.',
		'- Любую заметку можно **убрать крестиком**, если передумали. Заметок может быть несколько.',
	].join('\n'),
	'1.3.2': [
		'## 🔔 Звуки уведомлений: алерт + свой звук и редактор',
		'',
		'- **Короткий звук, когда работа агента остановилась**, а вы отошли: ход завершён, прогон встал (показана «Продолжить») или агент ждёт ответа/подтверждения плана. По умолчанию звучит только если окно IDE не в фокусе («позвать, когда отошёл»).',
		'- **Отдельный раздел «Уведомления» в настройках** — выбор звука с прослушиванием по клику, громкость и тумблеры событий.',
		'- **Редактор «VibeIDE Звуки»** (меню-мозг в заголовке): загрузите свой трек, наведите окно до 5 секунд на звуковую волну (клик по дорожке или бегунок сверху), прослушайте фрагмент и сохраните — обрезок ляжет в папку `sounds` рядом с данными IDE. Если выбранный свой звук пропал с диска, при старте вернётся стандартный с уведомлением.',
		'',
		'## 🪟 Модальные окна',
		'',
		'- Единые отступы и **адаптивная прокрутка** во всех модалках: полоса прокрутки появляется только когда содержимое не вмещается.',
		'',
		'## ⌨️ Проектные команды',
		'',
		'- Когда панель узкая и меню «Команды» сворачивается в «…», в нём снова доступны **«Редактировать команду…»** и **«Удалить команду…»**.',
		'',
		'## 🤖 Агент и надёжные правки',
		'',
		'- **Чище завершает работу.** Когда задача реально выполнена, автопилот раньше принимает завершение (через `vibe_complete`) и не гоняет модель лишними «продолжай!», из-за которых она могла выдумывать ненужную работу.',
		'- **`edit_file` выравнивает отступ блока по файлу**, если модель скопировала якорь без отступа — правка встаёт на свою колонку, а не «уезжает» влево.',
		'- **Убран протекающий тег `</think>`** в ответах моделей с отдельным каналом рассуждений.',
		'',
		'## 🧹 Меньше шума',
		'',
		'- **Убран навязчивый тост с рекомендованными расширениями воркспейса** — он всплывал при открытии проектов и мешал. Рекомендации по-прежнему доступны вручную на вкладке расширений.',
		'',
		'## 📦 Установщик',
		'',
		'- Установщик теперь называется **`VibeIDESetup.exe`** и публикуется от издателя **VibeBrains** с корректными ссылками на проект.',
	].join('\n'),
	'1.2.3': [
		'## 🩺 Диагностика провайдеров + быстрый «починить»',
		'',
		'- **Новая модалка «Проверка провайдеров»** (меню мозга → под «VibeIDE Команды»): показывает активные провайдеры (у кого прописан API-ключ) и послойно проверяет связь — конфиг, сеть, авторизация, каталог моделей — с задержкой ответа. Отчёт можно выгрузить в Markdown.',
		'- **«Починить связь»** — иконка-ключ в статус-баре снизу (и кнопка «Сбросить клиентов» в модалке) лечит ситуацию «токены вдруг перестали приходить у всех провайдеров» **без перезапуска IDE**: пересоздаётся сетевое соединение и сбрасываются кэши клиентов.',
		'',
		'## 💬 Чат и агент',
		'',
		'- **Контекст можно подмешать на лету.** Пока агент работает, наберите дополнение в поле ввода и нажмите Enter — оно учтётся на следующем шаге агента, без остановки хода.',
		'- **Прерывание на середине вызова инструмента исправлено** — больше не приводит к ошибке провайдера «duplicate tool_call id» при следующей отправке.',
		'- Слабые модели через агрегаторы надёжнее завершают ход: в XML-режиме им подсказывается точная форма вызова `vibe_complete`.',
		'- Надёжные правки файлов на слабых моделях: `edit_file` принимает простой формат «было/стало» даже когда поле блоков пустое.',
	].join('\n'),
	// 1.2.2 merges the never-published 1.2.1 highlights (see docs/knowledge/.../release-protocol.md):
	// a user updating 1.2.0 → 1.2.2 must still see 1.2.1's security work, so it's folded in here.
	'1.2.2': [
		'## 🚀 Надёжные правки файлов — даже на «слабых» моделях',
		'',
		'- **MiniMax и другие модели через агрегаторы больше не «тупят» на правках.** Раньше модель не могла выдать сложный блок правки и сваливалась в десятки одноразовых скриптов. Теперь у `edit_file` простой формат: **что заменить** (`old_string`) и **на что** (`new_string`) — два обычных поля.',
		'- **Терпимый поиск заменяемого текста** — правка применяется, даже если модель чуть ошиблась в отступах/пробелах или слегка перефразировала середину (line-trimmed + block-anchor по образцу OpenCode/Roo).',
		'- **Режим «Обзор» больше не зацикливается** — он read-only, и агент теперь прямо говорит «переключитесь на Агента, чтобы применить правки», а не долбит недоступный инструмент.',
		'- Рефактор-инструменты (rename/extract/generate_tests) теперь под тем же подтверждением, что и обычные правки, и исключены из read-only режимов.',
		'',
		'## 🔒 Безопасность',
		'',
		'- **Config Guard** — VibeIDE проверяет `.vibe/providers.json` и `mcp.json` при загрузке и предупреждает о небезопасном: незашифрованные endpoint-ы, секреты в открытом виде, запуск удалённых скриптов в MCP. Новая команда «Config Guard — показать находки».',
		'- **`.vibe/ignore` теперь действительно работает** — файлы из него агент больше не читает и не ищет (например минифицированные бандлы в одну строку, которые раздувают контекст). Читайте `-debug`/исходники.',
		'- **`edit_file` больше не теряет код** — незавершённый блок правки отклоняется с подсказкой, а не применяется как удаление найденного фрагмента.',
		'- **`browse_url` починен** — снова открывает веб-страницы (была сломана передача URL, инструмент падал на любом вызове).',
		'',
		'## 🐛 Исправления',
		'',
		'- Восстановлена подсветка наведения и текущей позиции (стрелки ↑/↓) в меню `/skill:`.',
		'- Аудит-лог переехал в **`.vibe/audit.jsonl`** — одна служебная папка в проекте вместо двух.',
	].join('\n'),
	'1.2.0': [
		'## ✨ Свои LLM-провайдеры — теперь полноценные',
		'',
		'Провайдеры и модели настраиваются файлом **`.vibe/providers.json`** — без пересборки IDE, и работают **как встроенные**:',
		'',
		'- **Карточка в «Облачных провайдерах»**: поле ключа, проверка валидности (✓ действителен / ✗ недействителен) и источник (`.vibe/.env` или введён в IDE).',
		'- **Живой каталог моделей** из `<baseURL>/v1/models` — перечислять модели руками не нужно (`models.fetch`).',
		'- **Вкладка «Модели» с тумблерами** — включаете нужные, в выбор чата идут только они; модели с параметрами из файла помечены `✎`.',
		'- **Возможности по имени модели**: vision, reasoning, формат инструментов и контекст берутся из базы знаний так же, как у встроенных.',
		'- **Патч встроенных** по `id`, **клон** через `extends`, порядок через `order`, тумблеры `active`; IntelliSense + диагностика в редакторе.',
		'',
		'Рецепты и пример — в `.vibe/providers.example.jsonc`.',
	].join('\n'),
};

/** Strip a leading `v` and surrounding whitespace from a version string. */
function normalizeVersion(version: string): string {
	return version.trim().replace(/^v/i, '');
}

/**
 * Highlights Markdown for the given `vibeVersion`, or `undefined` when there's nothing to announce
 * for it. The contribution treats `undefined` as «don't show the modal».
 */
export function getWhatsNewForVersion(version: string | undefined): string | undefined {
	if (!version) { return undefined; }
	return WHATS_NEW_BY_VERSION[normalizeVersion(version)];
}
