# Bundled dispatcher + companion scripts are regenerated by `npm run sync`
# (see bin/sync-from-repo.js). They are published from the registry tarball
# but not committed.
scripts/
node_modules/
*.log
