# Donation Phrases — фразы блока «Поддержать проект» в релизах

База фраз для блока «Поддержать проект» в GitHub Releases.
Workflow выбора и сохранения — в [AGENTS.md → «Фраза поддержки в релизах»](../AGENTS.md).

Перед каждой фразой блока всегда идёт постоянная вступительная строка:

> Если VibeIDE оказалось полезным — буду рад благодарности.

Ниже — варианты второй строки (юмористическая подпись).

---

## Активные (готовы к использованию)

1. VibeIDE — opt-in донаты, opt-out телеметрия. Всё, как вы любите.
2. Лицензия MIT, подписки нет, бэкдоров не завезли. Если хочется отблагодарить — кнопка ниже.
3. Open source держится на трёх китах: Linux, Git и донатах.
4. Текст в код-блоках больше не ливёт по Шрёдингеру — DOM есть, пиксели тоже. Автор по выходным выбирает тот же режим.
5. Watchdog следит за памятью каждые 5 минут. У автора такая же периодичность — но за кофе. Кнопка ниже синхронизирует расписания.

## Отложенные (не вошли, можно вернуться)

- Сначала маскировал плейсхолдером, потом нашёл корень. Пользователь сказал "магией пахнет". Автор тоже периодически нюхает симптомы — кнопка ниже всё ставит на место.
- DeepSeek требовал `reasoning_content` обратно — теперь получает на каждом ходе. Автор тоже требует кофе обратно. Кнопка ниже доставляет.
- `/var` больше не подсвечивается как скилл. Подсвечивается только то, что реально умеет — `/skill:*`. Автор тоже работает только на том, что умеет — кофе, кофе и кнопка ниже.
- Cursor берёт $20 в месяц, мы — ноль. Если разница на счётчике приятна, частью можно поделиться.
- VibeIDE отлично умеет конвертировать кофе в фичи. Курс выгодный.
- Подписки нет, телеметрии нет, рекламных баннеров не будет. А кофе у автора всё-таки заканчивается.
- Если сэкономили на подписке Cursor / Copilot / Windsurf — часть сэкономленного можно отправить в фонд имени автора. Фонд маленький, но кофе любит.
- Cursor стоит $240 в год, Copilot — $120, Windsurf — $180. VibeIDE — 0₽ и ваш API-ключ. Разницу можно частично округлить в сторону автора.
- Мы не берём $20 в месяц. Но если у вас завалялся $1 — автор не откажется.
- Считайте это «Cursor Pro» без Cursor и без Pro. Подписку на это решение можно оформить ниже одним платежом и в любую сторону.
- Разработчик гарантированно потратит донат на: кофе, домен, кофе, или новую фичу. В указанном порядке вероятностей.
- Каждый донат превращается в один коммит. Иногда даже работающий.
- Автор пишет это в три часа ночи. Кофе закончился полчаса назад. Делайте выводы.
- Поймали вайб? Поделитесь вайбом обратно.
- VibeIDE — бесплатно. Хорошее настроение автора — нет.
- Если IDE зашло — закиньте автору на следующую бессонную ночь.
- Подписки нет, рекламы нет, кофе у автора — пока тоже нет.
- Платный план не появится. Кнопка ниже — единственный «pro tier».
- Каждый донат превращается в один коммит. Иногда — в пончик. Реже — в работающий пончик.
- Подписки нет, телеметрии нет. Печенек у автора — пока тоже нет, но это поправимо.
- crypt32.node собран. Кейк всё ещё не съеден. Несправедливо.
- VibeIDE отлично конвертирует пончики в фичи. Сертификаты — в TLS-handshake'и. Курс выгодный.
- crypt32.node — это четыре байта надежды, которых не было в инсталлере. Теперь есть. Кофе понимает иронию.
- Корп-сеть сломала VibeIDE. VibeIDE сломал корп-сеть обратно — через build/Release/crypt32.node. Ничья.
- Корневые сертификаты Windows наконец-то увидели Node.js. Раньше они только переписывались — теперь и здороваются.
- Сертификаты теперь подгружаются. Кофе — пока ручной режим.
- Anti-shell гарды, 2M токенов, autopilot auto-reset. Чтобы автор тоже не зависал — кнопка ниже.

## История использования

| Версия | Фраза |
|---|---|
| v0.2.0 | VibeIDE — opt-in донаты, opt-out телеметрия. Всё, как вы любите. |
| v0.3.0 | Лицензия MIT, подписки нет, бэкдоров не завезли. Если хочется отблагодарить — кнопка ниже. |
| v0.4.1 | Open source держится на трёх китах: Linux, Git и донатах. |
| v0.4.2 | Поймали вайб? Поделитесь вайбом обратно. |
| v0.5.0 | Мы не продаём подписки. Мы продаём вайб. Оплата добровольная. |
| v0.5.2 | Каждый донат превращается в один коммит. Иногда даже работающий. |
| v0.5.3 | Разработчик гарантированно потратит донат на: кофе, домен, кофе, или новую фичу. В указанном порядке вероятностей. |
| v0.6.0 | Vibe Driven Development: шестой минор, пятый литр кофе, ноль подписок. Кнопка ниже баланс выравнивает. |
| v0.7.0 | Новый релиз — новые дыры — новые фиксы. Круговорот идеален, но кофе требуется всё равно. |
| v0.7.2 | Чем больше фиксов, тем больше было багов. Совпадение? Не думаю. Кофе не совпадает само по себе. |
| v0.7.3 | Один баг — три дня. Три дня — одна пачка печенек. Математика проекта прозрачная. |
| v0.7.5 | Anti-shell гарды, 2M токенов, autopilot auto-reset. Чтобы автор тоже не зависал — кнопка ниже. |
| v0.7.6 | Растет виджет в истории чата — растёт число токенов, кофе и баг-репортов. Баланс уважаем. |
| v0.7.7 | Текст в код-блоках больше не ливёт по Шрёдингеру — DOM есть, пиксели тоже. Автор по выходным выбирает тот же режим. |
| v0.8.0 | OpenRouter был недоступен. VibeIDE это понял. Автор тоже понял. Кофе понимало первым. |
| v0.8.1 | Релиз про один файл на 144 KB. Один файл — три дня диагностики. Кофе в курсе всех подробностей. |
| v0.8.2 | Растет виджет в истории чата — растёт число токенов, кофе и баг-репортов. Баланс уважаем. |
| v0.9.0 | Тулы теперь живут по своим файлам, как и должно быть. Автор живёт по своему графику — кнопка ниже его выравнивает. |
| v0.9.1 | Добавили тул invalid — единственный, который ничего не делает. Моделям понравилось. Автор ищет свой invalid на выходных. |
| v0.9.2 | Модели начали звать тулы цифрами "0", "5", "20". VibeIDE научился их перехватывать через invalid. Автор пока перехватывает только кофе — кнопка ниже выравнивает баланс. |
| v0.9.3 | 0.9.2 сломал recovery — выбросил список тулов из ошибки. 0.9.3 вернул. Кофе у автора регрессии не умеет — кнопка ниже чинит баланс. |
| v0.9.4 | `Object.keys(array)` в JavaScript возвращает `['0', '1', '2', ...]`. Десять часов выяснения. Автор теперь тоже объект с ключами `['кофе', 'кофе', 'кофе']`. Кнопка ниже добавляет следующий. |
| v0.10.0 | `/skill:NAME` теперь выгружается в контекст целиком. Кофе у автора выгружается аналогично — кнопка ниже даёт refill. |
| v0.11.0 | Контекст-индикатор больше не врёт в 50×. Реальные токены провайдера → реальные числа. Счётчик кофе у автора тоже честный — кнопка ниже его обновляет. |
| v0.12.0 | MAX_FILES_READ_PER_QUERY ушла в историю — модель может читать сколько нужно. Лимит на кофе у автора пока в силе — кнопка ниже расширяет квоту. |
| v0.12.2 | Три регрессии из v0.10–0.12. Три фикса. Ноль новых фич. Релиз чистой совести. У автора совесть тоже чистая — кофе допито. Кнопка ниже пополняет. |
| v0.12.3 | Ночной OOM, петля invalid_params, ContextGuard-спам, имена провайдеров, валидатор `dir`, сброс счётчика. Шесть фиксов одним релизом. У автора шестой кофе того же дня — кнопка ниже компенсирует. |
| v0.12.4 | Каждый hardcode превратился в setting. Каждая магия — в feature-flag. Каждый leak — в `deleteThread` cleanup. Релиз-аудит чистой совести. Кофе у автора пока без feature-flag — кнопка ниже включает. |
| v0.12.5 | Шестой audit-pass нашёл то, что пять предыдущих пропустили: один и тот же список из 16 провайдеров жил inline в четырёх файлах. Теперь — один const. У автора рефактор кофейной зависимости пока не завезён — кнопка ниже временно компенсирует. |
| v0.13.1 | Ctrl+K теперь понимает `/doc`, `/refactor`, `/tests`, `/explain`, `/fix`. Автор понимает только `/кофе` и `/отпуск` — второе пока не приходит, кнопка ниже компенсирует первое. |
| v0.13.5 | Скрытый сканер тихо стучался в socket.dev на каждый старт, искал в npm пакеты с именами `vscode.debug-auto-launch` — таких на npm нет и не будет. Уволен. У автора кнопка ниже ничего не сканит — просто работает. |
| v0.13.7 | Таблица квирков моделей переехала из TS-кода в JSON на CDN. Теперь новая «странная» модель — это PR на один файл, а не релиз IDE. У автора кофе пока тоже на CDN, но без ETag-кэша. |
| v0.13.8 | Сырые XML-теги в чате выбывают в полаконичный placeholder. Раньше лезли в UI как `<get_dir_tree><uri>...</uri></get_dir_tree>` — теперь нет. Чат чище. У автора в голове тоже safety net — нераспарсенные мысли прячутся в плейсхолдере «потом». Кнопка ниже распарсивает. |
| v0.13.9 | Watchdog следит за памятью каждые 5 минут. У автора такая же периодичность — но за кофе. Кнопка ниже синхронизирует расписания. |
| v0.13.10 | Модели начали слать `<read_file path="..." />` напрямую. Парсер не знал — теперь знает. У автора тоже бывают self-closing мысли, кнопка ниже их раскрывает. |
| v0.13.12 | Pure helpers покрыты тестами под завязку, UI smoke ждёт релизного билда. У автора покрытие кофе пока 90% — кнопка ниже добивает. |
| v0.13.14 | Toast можно закрыть не глядя, модал — нужно прочитать. У автора с кнопкой ниже та же дисциплина: проигнорировать сложнее. |
| v0.13.15 | Модал по центру, workbench не блокируется. Автор тоже старается не блокировать — особенно пока кофе ещё есть. Кнопка ниже не блокирует ничего, но привлекает внимание. |
| v0.13.16 | Агент перестал замирать после «Начинаю...» и выпрашивать «продолжи» — теперь доводит шаг сам. У автора роль «продолжи» исполняет кофе; кнопка ниже его пополняет. |
| v0.13.17 | Каталог квирков моделей теперь берётся из самого свежего источника — рядом с exe, из сборки или с CDN, по дате. У автора самый свежий источник один — кофе по дате обжарки. Кнопка ниже пополняет запас. |
| v0.13.18 | Модели звали `file_read` вместо `read_file` и слали аргументы от чужого тула — теперь чинится и имя, и аргументы на лету. Автор после третьего кофе тоже путает аргументы; кнопка ниже спонсирует четвёртый. |
| v0.13.19 | Модель писала имя тула то `file_read`, то `FileRead`, то `ReadFile` — теперь все написания сводятся к одному концепту, без погони за каждым. Автор пишет «кофе», «кофэ» и «☕» — кнопка ниже резолвит всё в одно. |
| v0.13.20 | Три релиза подряд модели изобретали новый формат тега, чтобы не вызывать инструмент — пришлось перевести их на структурный канал, где «фантазировать» нечем. Автор тоже фантазирует креативнее без кофе; кнопка ниже возвращает его в структурный режим. |
| v0.13.21 | Хост на секунду задумался — а спасатель уже бьёт тревогу и зовёт эвакуацию, по кругу. Научили его сначала досчитать до пятнадцати. Автору для той же выдержки нужен кофе; кнопка ниже наливает. |
| v0.13.22 | Раньше «думает молча» = «завис, перезапускай»; теперь сначала проверяем, живо ли соединение, и не дёргаем мыслящего. Плюс поставили в код секундомер, чтобы не гадать. Автору секундомер тоже не помешает — особенно к третьему литру кофе; кнопка ниже спонсирует. |

---

## Полный шаблон блока (для копирования в release notes)

```markdown
---

### Поддержать проект

Если VibeIDE оказалось полезным — буду рад благодарности.
<!-- сюда вставляется одна активная фраза -->

<a href="https://raw.githubusercontent.com/borodatych/VSCodeSyncFiles/main/media/QR-Code.jpg" target="_blank">
  <img src="https://raw.githubusercontent.com/borodatych/VSCodeSyncFiles/main/media/QR-Code.jpg" width="120" alt="QR-код для поддержки проекта">
</a>
```
